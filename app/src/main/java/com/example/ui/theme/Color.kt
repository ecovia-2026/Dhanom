package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// NOTE: Theme colors now live in ThemePalettes.kt and are consumed via
// MaterialTheme.colorScheme (see Theme.kt). This file only keeps the few
// semantic constants still referenced across modules.
val ColorSuccess = Color(0xFF2E7D32)
val ColorDanger = Color(0xFFD32F2F)
